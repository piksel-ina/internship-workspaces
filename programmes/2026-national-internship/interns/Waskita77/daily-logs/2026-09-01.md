## Done

- Deployed `piksel-core` on Ubuntu WSL 2 using Docker Compose and configured the production PostgreSQL port to `5433`.
- Started Open Data Cube (ODC), registered 17 products, and validated STAC integration with Earth Search using an isolated test stack.
- Indexed Sentinel-2 L2A dataset through STAC integration and verified the ODC workflow.
- Repaired PostgreSQL index corruption using `REINDEX`, validated database integrity with `pg_amcheck`, and created a pre-repair backup with SHA-256 checksum verification.
- Investigated Docker CLI and daemon instability, including parsing failures, hangs, and segmentation faults across multiple Docker versions and WSL configurations.
- Downgraded WSL to version 2.7.0, restored the bundled WSL kernel, and reviewed memory, swap, CPU, networking, and Docker MTU configurations.
- Performed controlled stability tests across different system configurations to identify runtime failure patterns.
- Increased ODC health-check timeout to 30 seconds and performed Docker/WSL recovery steps to restore PostgreSQL and ODC services.

## Blocker / question

- Docker still experiences intermittent segmentation faults and hangs under certain runtime conditions.
- In some recovery scenarios, `dockerd` crashed and automatic restart did not fully restore container tasks, requiring manual Docker and container recovery before final ODC validation.
- The root cause of Docker instability has not been confirmed and further investigation is required across Docker runtime, WSL environment, and system-level configurations.
- Final verification of ODC healthy status after recovery and timeout changes is still pending.

## Next

- Complete Docker and containerd recovery, then verify Piksel containers, PostgreSQL port `5433`, and ODC system health.
- Continue diagnosis using bounded commands with timeouts and controlled A/B testing across different software and environment configurations.
- Collect additional Docker, WSL, and kernel logs if segmentation faults or runtime hangs occur again.
