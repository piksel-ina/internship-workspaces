## Done

- Continued operating the `piksel-core` deployment on Ubuntu WSL 2 with Docker Compose.
- Checked the PostgreSQL/PostGIS and Open Data Cube services, including container state, health checks, database connectivity, and PostgreSQL port availability.
- Consolidated operational test results into Markdown and CSV summaries for easier deployment review.
- Separated application health from Docker CLI failures by verifying the Docker Engine API, Docker socket, service status, and application ports independently.
- Confirmed that `piksel-postgres-1` and `piksel-odc-1` were `running/healthy` and that PostgreSQL was available on host port `5433`.
- Confirmed that Docker Engine and containerd were active and that the Docker Engine API responded to health and status requests.
- Reviewed the user's manual Compose output and identified that the repository-root `.env` file was not loaded when commands were executed from `~/piksel-core/docker`.
- Provided corrected operational commands using `--env-file ../.env` for PostgreSQL, ODC, product listing, product-count queries, Earth Search connectivity, and container logs.
- Ended additional stability testing and returned the work scope to `piksel-core` application functionality and operations.
- Established the next migration target: native Ubuntu 24.04 in a dual-boot configuration, with WSL and Docker runtime instability serving as one of the technical reasons supporting the migration.

## Blocker / question

- Docker CLI in WSL can still produce parsing failures, segmentation faults, corrupted output, or hangs and is not always a reliable source of deployment status.
- Intermittent WSL and Docker issues can disrupt container management even while application services remain available, reducing the reliability of WSL for long-term deployment operations.
- Initial manual checks did not load `POSTGRES_USER`, `POSTGRES_DB`, and `POSTGRES_PASSWORD` because Compose was not given the root `.env` file.
- Further stability diagnosis was discontinued so subsequent work can remain focused on deployment and migration.

## Next

- Keep the current WSL deployment available as the migration source; ODC and PostgreSQL are currently `running/healthy`, with PostgreSQL exposed on host port `5433`.
- Include `--env-file ../.env` in Compose commands issued from `~/piksel-core/docker`.
- Migrate `piksel-core` to native Ubuntu 24.04 on the planned dual-boot installation.
- Use the native Linux environment to reduce the Windows–WSL integration and virtualization layers involved in Docker operations, simplifying future operation and diagnosis of the deployment.
- Before migration, inventory the deployment configuration and prepare a PostgreSQL backup, backup checksum, `.env` copy, Docker Compose changes, and the required image/volume list.
- On native Ubuntu, install Docker Engine and Compose, clone the repository, restore configuration and database data, and verify ODC, PostGIS, registered products, and Earth Search connectivity.
