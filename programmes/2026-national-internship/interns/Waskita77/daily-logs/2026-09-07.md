## Done

- Isolated recurring crashes and Docker runtime instability to logical CPUs 10 and 11 (physical Core 6) of the AMD Ryzen 5 5500U during 64-bit memory copy operations (`rep movsq`); mitigated via `msconfig` (5-core / 10-thread profile) and restored the primary workspace to WSL 2 running Docker Engine.
- Completed container builds (`make build-all-dev`) after resolving `piksel-jupyter` blockers (pinned `pyTMD@2.2.8` and added public DNS in `daemon.json`), then initialized the ODC PostGIS database (`make init-db`) and registered all product definitions (`make all-products`).
- Indexed 534 Sentinel-2 L2A datasets via Element84 STAC API, refreshed Datacube Explorer footprints, and passed all 15 unit and integration tests (`make test-verbose`) after addressing port 5432 collisions and S3 crawling timeouts.
- Mapped 21 repositories across the `piksel-ina` organization, stabilized JupyterLab runtime dependencies (`pystac==1.14.3`), applied `validate_connection=False` as a temporary schema validation bypass, and diagnosed private S3 credential requirements on SCL band assets.
- Validated the end-to-end analytical workflow on Sentinel-2 L2A (May 28, 2024 scene) using `group_by="solar_day"` to mosaic swath boundaries, rendering contrast-stretched True Color (RGB), and calculating NDVI across 2.76 million valid pixels.

## Blocker / question

- Product asset definitions for `s2_l2a` reference private `s3://` bucket paths for SCL JP2 data requiring AWS authentication, preventing direct loading alongside public HTTPS COG spectral bands.

## Next

- Align ODC library versions across container images to remove the temporary schema validation bypass.
- Align STAC asset mappings or product definitions for SCL to reference public COG endpoints.
- Export processed NDVI arrays as Cloud-Optimized GeoTIFFs (COG) and verify output spatial metadata.