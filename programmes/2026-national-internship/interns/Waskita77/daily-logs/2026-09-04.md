## Done

- Continued stability testing of the native Ubuntu `piksel-core` deployment.
- Checked Docker Compose service status and confirmed that PostgreSQL/PostGIS, ODC, Explorer, and Jupyter were running and `healthy`.
- Verified Explorer access on port `5000`, Jupyter access on port `8888`, PostgreSQL connectivity, the `s2_l2a` Explorer product page, and the indexed Sentinel-2 L2A dataset count.
- Confirmed that the `s2_l2a` product still contains 534 datasets.
- Reviewed the `piksel-ina` repositories to understand how `piksel-core` relates to the Jupyter image, OWS, Explorer, data pipelines, and infrastructure configuration.

## Blocker / question

- None.

## Next

- Continue testing the Jupyter-side ODC workflow.
- Keep deployment findings in separate notes as evidence for future issue reporting or repository contributions.
